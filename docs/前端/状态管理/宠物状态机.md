# 宠物状态机

## 职责

管理宠物的行为状态流转：待机→关注→交互→说话。状态机驱动物理窗口的鼠标穿透状态切换。

> 状态机核心逻辑在 PetCore 内核（`petcore/internal/fsm/`）中实现，前端通过 Tauri 事件消费状态变化。

## 状态定义

```
                    ┌──────────┐
          ┌────────►│  待机     │◄──────────────┐
          │         │ (穿透开)  │                │
          │         └────┬─────┘                │
          │              │                      │
      对话结束         检测到用户               超时
          │              │                      │
          │         ┌────▼─────┐                │
          ├────────►│  关注     │────────────────┤
          │         │ (穿透关, │                │
          │         │  转向鼠标)│               │
          │         └────┬─────┘                │
          │              │                      │
          │         用户对话/点击                 │
          │              │                      │
          │         ┌────▼─────┐                │
          │         │  交互     │────────────────┤
          │         │ (穿透关, │                │
          │         │  表情动画)│               │
          │         └────┬─────┘                │
          │              │                      │
          │         AI 开始回复                   │
          │              │                      │
          │         ┌────▼─────┐                │
          │         │  说话     │                │
          │         │ (打字机  │────────────────┤
          │         │  效果)   │                │
          │         └──────────┘                │
          └─────────────────────────────────────┘
```

## 每个状态对应的窗口行为

| 状态 | 鼠标穿透 | 置顶 | 动画 |
|------|---------|------|------|
| 待机 | ✅ 开启 | ✅ | 随机小动作（眨眼、晃头） |
| 关注 | ❌ 关闭 | ✅ | 转向鼠标方向 |
| 交互 | ❌ 关闭 | ✅ | 表情随对话变化 |
| 说话 | ❌ 关闭 | ✅ | 口型同步 |

## 转换规则

```
idle ──(EventStateChanged)──→ attention
idle ──(EventPetSpeak)──→ speaking
attention ──(EventStateChanged)──→ interaction
attention ──(EventPetSpeak)──→ speaking
attention ──(EventError)──→ idle
interaction ──(EventAgentReply)──→ speaking
interaction ──(EventError)──→ idle
speaking ──(EventStateChanged)──→ idle
```

## 核心代码

| 文件路径 | 说明 |
|---------|------|
| `petcore/internal/fsm/machine.go` | FSM 状态机（Machine 接口 + MockMachine 实现 + transitionRules） |
| `petcore/internal/fsm/fsm_test.go` | 13 条转换规则全覆盖测试 |
| `src-tauri/src/window.rs` | 鼠标穿透切换与窗口状态联动 |
