# 路由配置

## 窗口管理

桌面端应用无传统路由导航。窗口切换通过 Electron 主进程管理：

| 窗口 | 创建方式 | 说明 |
|------|---------|------|
| 主宠物窗口 | 应用启动时自动创建 | 透明置顶窗口，显示 Live2D + UI |
| 设置面板 | 右键菜单→设置 | 普通窗口，不透明，可拖拽 |

## 窗口间通信

通过 Electron IPC 或 WebSocket 消息：
- 主进程 ↔ 渲染进程：`ipcMain` / `ipcRenderer`
- 前端 ↔ 后端：WebSocket（127.0.0.1:8765）
